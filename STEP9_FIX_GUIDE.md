# 로또 9단계 - 번호 추출 화면 소실 수정판

## 확인된 위험 지점
- 번호 생성 결과를 Pandas DataFrame 그대로 `st.session_state`에 저장
- 후보 5,000~10,000개까지 한 번에 생성 가능
- 모바일/Streamlit Cloud 저메모리 환경에서는 세션 재실행 또는 프로세스 재시작 위험
- 생성/렌더링 실패 시 사용자가 '화면 전체가 사라진 것'처럼 느낄 수 있음

## 이번 수정
1. `last_picks` DataFrame 세션 저장 제거
2. 추천 결과만 `list[dict]` 형태로 저장
3. 분석 후보 수를 500/1000/2000/3000으로 제한
4. 내부적으로도 최대 3000개로 강제 제한
5. 생성 실패 시 이전 추천 결과 복구
6. 렌더링 오류를 게임 단위로 격리
7. 구버전 세션의 DataFrame 추천 결과를 자동 변환
8. 버튼 문구를 `이번 회차 번호 추출`로 통일

## GitHub 교체 파일
- mobile_app.py
- db.py
- generator.py
- requirements.txt

## 적용
1. ZIP 압축 해제
2. GitHub 저장소에 위 4개 파일 덮어쓰기
3. Commit changes
4. Streamlit Manage app > Reboot app
5. 브라우저/앱 새로고침

## 테스트
1. 앱 첫 화면 정상 출력
2. 번호 생성 탭 진입
3. 분석 후보 수 1000으로 시작
4. `이번 회차 번호 추출` 실행
5. 5게임 추천 카드가 표시되는지 확인
6. 추천번호 저장
7. 다시 번호 추출해 화면이 유지되는지 확인
