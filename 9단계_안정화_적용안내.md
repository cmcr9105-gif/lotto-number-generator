
# 9단계 안정화 점검판 적용

## 이번에 GitHub에서 교체할 파일
1. `mobile_app.py`
2. `db.py`
3. `generator.py`
4. `requirements.txt`

`streamlit_app.py`는 기존 것이 `from mobile_app import *`라면 그대로 사용해도 됩니다.

## 중요한 수정
- 최신 회차 1건 저장 뒤 전체 화면이 사라지는 흐름 제거
- DB가 비어 있어도 화면을 유지
- 자동 200회 구축을 첫 화면에서 제거
- 직접 입력 메뉴를 항상 표시
- 입력 번호를 1→2→3→4→5→6→보너스 순으로 표시
- DB busy timeout 및 SQLite 복구 안정성 개선
- 잘못된/중복 번호 저장 방지
- 저장 성공 메시지를 재실행 뒤에도 표시
- 데이터가 1~9회뿐일 때 앱은 유지하면서 통계 신뢰도 경고
- 번호 생성 후보 기본값을 줄여 Streamlit Cloud 과부하 완화
- 번호 생성/통계/당첨대조/성과 화면별 오류를 격리
- 자동조회 실패가 전체 앱을 중단하지 않음

## 적용 순서
1. 교체 ZIP 압축 풀기
2. GitHub > Upload files
3. 위 4개 파일 업로드
4. Commit changes
5. Streamlit > Manage app > Reboot app
6. 앱 새로고침

## 테스트 순서
1. DB가 비어 있으면 직접 입력 메뉴가 보이는지 확인
2. 회차 1건 저장
3. 저장 후 앱 제목/상태/번호 생성 탭이 그대로 보이는지 확인
4. 번호 생성 5게임 실행
5. 추천번호 저장
6. 같은 대상회차 당첨번호 입력 후 당첨대조 확인
